#import (Config.path +'/Kernel/Template/View/MasterView') = MasterView;

class BodyLeftTemplateMainView extends MasterView {

	render : function () {
		var elements = [
			this.getTemplate('body_left_logo_template', this._template)
		];
		return (<div id="left" key={this._template.getViewClassName()}>
			{elements.map(function(elem, key){
				return elem;
			})}
		</div>);
	}

}

#export BodyRightTemplateMainView;